
process {

    withName: 'Branch1:Command1Software1:.*' {
    
        container = {
            if (!task.ext.version || task.ext.version.equals('X.X')){

                }
            }
    
        }

    withName: 'Branch2:Command1Software1:.*' {
    
        container = {
            if (!task.ext.version || task.ext.version.equals('X.X')){

                }
            }
    
        }

    withName: 'Branch2:Command2Software2:.*' {
    
        container = {
            if (!task.ext.version || task.ext.version.equals('X.X')){

                }
            }
    
        }

    ////LEAF_CONFIG////

    }
